name: Docker

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    name: Test Commit Workflow Changes
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Commit Workflow Changes
        uses: d3code/github-action-commit-workflow-changes@eea45ab132bc4662545d45ce5bd9ca89982be068
