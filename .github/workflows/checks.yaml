name: Docker

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    name: Test Commit Workflow Changes
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Commit Workflow Changes
        uses: d3code/github-action-commit-workflow-changes@c5aebe488f8a0212c7473ad5e907c19635abc1f6
