name: Docker

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    name: Test Commit Workflow Changes
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Commit Workflow Changes
        uses: d3code/github-action-commit-workflow-changes@d137ebe96acd1118cc28bfededbe952cb23adfbe
  
